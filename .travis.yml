dist: xenial
language: node_js
cache: npm
notifications:
  email: false
node_js:
  - '9'
before_script:
  - npm prune
script:
  - npm run test
after_success:
  - npm run report-coverage
deploy:
  - provider: script
    script: npx semantic-release -r
      https://${GH_TOKEN}@github.com/${TRAVIS_REPO_SLUG}.git
    skip_cleanup: true
    keep_history: true
    on:
      branch: master
branches:
  only:
  - master
addons:
  chrome: stable
services:
  - xvfb
install:
  - npm install
  